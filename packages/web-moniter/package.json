{
  "name": "@aero/web-moniter",
  "version": "0.2.4",
  "description": "Web监控SDK入口包 - 整合所有功能",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "keywords": [
    "web-monitoring",
    "sdk"
  ],
  "author": "senmu",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@aero/core": "workspace:*",
    "@aero/plugins": "workspace:*",
    "@aero/reporter": "workspace:*",
    "@aero/types": "workspace:*"
  }
}
